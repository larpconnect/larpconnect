package org.larpconnect.integration;

/** Marker class to identify the integration test harness package. */
public final class IntegrationMarker {
  /**
   * Helper to verify active state.
   *
   * @return true if active.
   */
  public boolean isActive() {
    return true;
  }
}
